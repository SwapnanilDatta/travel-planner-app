[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "channels>=4.3.2",
    "daphne>=4.2.2",
    "django>=6.0.6",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[tool.uv.workspace]
members = [
    "microservices",
]
